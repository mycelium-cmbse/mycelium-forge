// ------------------------------------------------------------------------------------------------
// <copyright file="OrganizationPermissionService.cs" company="Starion Group S.A.">
//
//   Copyright 2026 Starion Group S.A.
//   SPDX-License-Identifier: Apache-2.0
//
// </copyright>
// ------------------------------------------------------------------------------------------------

// ------------------------------------------------------------------------------------------------
// --------THIS IS AN AUTOMATICALLY GENERATED FILE. ANY MANUAL CHANGES WILL BE OVERWRITTEN!--------
// ------------------------------------------------------------------------------------------------

namespace Mycelium.Forge.Dal.AutoGenPermissionService
{
    using System.CodeDom.Compiler;

    using FluentResults;

    using Mycelium.Forge.Common;
    using Mycelium.Forge.Dal.AutoGeneratedServices;
    using Mycelium.Forge.Dal.PermissionService;

    /// <summary>
    /// Default permission service for <see cref="IOrganization"/>.
    /// </summary>
    [GeneratedCode("Mycelium.Forge.Generator", "1.0.0")]
    public partial class OrganizationPermissionService : PermissionServiceBase<IOrganization>, IOrganizationPermissionService
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="OrganizationPermissionService"/> class.
        /// </summary>
        public OrganizationPermissionService()
        {
        }

        /// <summary>
        /// Core implementation hook for verifying create permissions on <paramref name="toCreate"/>.
        /// </summary>
        /// <param name="userContext">The contextual user information and assigned roles.</param>
        /// <param name="toCreate">The entity to create.</param>
        /// <returns>An awaitable <see cref="Task{Result}"/> indicating whether creation is permitted.</returns>
        protected override Task<Result> IsAllowedToCreateImplementation(IUserContext userContext, IOrganization toCreate)
        {
            return Task.FromResult(PermissionGuard.GuardPermission(userContext, PermissionKind.CreateOrganization));
        }

        /// <summary>
        /// Core implementation hook for verifying read permissions on <paramref name="thing"/>.
        /// </summary>
        /// <param name="userContext">The contextual user information and assigned roles.</param>
        /// <param name="thing">The entity to read.</param>
        /// <returns>An awaitable <see cref="Task{Result}"/> indicating whether reading is permitted.</returns>
        protected override Task<Result> IsAllowedToReadImplementation(IUserContext userContext, IOrganization thing)
        {
            if (userContext.AccountId.HasValue && thing.Administrator.Contains(userContext.AccountId.Value))
            {
                return Task.FromResult(Result.Ok());
            }

            if (userContext.AccountId.HasValue && thing.Member.Contains(userContext.AccountId.Value))
            {
                return Task.FromResult(Result.Ok());
            }

            return Task.FromResult(PermissionGuard.GuardAnyPermission(userContext, PermissionKind.ViewAllOrganizations, PermissionKind.ManageOrganizations));
        }

        /// <summary>
        /// Core implementation hook for verifying update permissions comparing <paramref name="existingThing"/> and <paramref name="updatedThing"/>.
        /// </summary>
        /// <param name="userContext">The contextual user information and assigned roles.</param>
        /// <param name="existingThing">The existing persisted entity state.</param>
        /// <param name="updatedThing">The updated entity state.</param>
        /// <returns>An awaitable <see cref="Task{Result}"/> indicating whether updating is permitted.</returns>
        protected override Task<Result> IsAllowedToUpdateImplementation(IUserContext userContext, IOrganization existingThing, IOrganization updatedThing)
        {
            if (!existingThing.Administrator.SequenceEqual(updatedThing.Administrator))
            {
                var guard = PermissionGuard.GuardPermission(userContext, PermissionKind.TransferOrganizationAdministration);

                if (guard.IsFailed)
                {
                    return Task.FromResult(guard);
                }
            }

            if (!existingThing.Member.SequenceEqual(updatedThing.Member))
            {
                var guard = PermissionGuard.GuardAnyPermission(userContext, PermissionKind.InviteOrganizationMembers, PermissionKind.ManageOrganizations);

                if (guard.IsFailed)
                {
                    return Task.FromResult(guard);
                }
            }

            if (existingThing.DefaultPackageVisibility != updatedThing.DefaultPackageVisibility)
            {
                var guard = PermissionGuard.GuardAnyPermission(userContext, PermissionKind.ConfigureDefaultPackageVisibility, PermissionKind.ManageOrganizations);

                if (guard.IsFailed)
                {
                    return Task.FromResult(guard);
                }
            }

            if (userContext.AccountId.HasValue && existingThing.Administrator.Contains(userContext.AccountId.Value))
            {
                return Task.FromResult(Result.Ok());
            }

            return Task.FromResult(PermissionGuard.GuardAnyPermission(userContext, PermissionKind.ManageOrganizationSettings, PermissionKind.ManageOrganizations));
        }

        /// <summary>
        /// Core implementation hook for verifying delete permissions on <paramref name="thing"/>.
        /// </summary>
        /// <param name="userContext">The contextual user information and assigned roles.</param>
        /// <param name="thing">The entity to delete.</param>
        /// <returns>An awaitable <see cref="Task{Result}"/> indicating whether deletion is permitted.</returns>
        protected override Task<Result> IsAllowedToDeleteImplementation(IUserContext userContext, IOrganization thing)
        {
            if (userContext.AccountId.HasValue && thing.Administrator.Contains(userContext.AccountId.Value))
            {
                return Task.FromResult(Result.Ok());
            }

            return Task.FromResult(PermissionGuard.GuardPermission(userContext, PermissionKind.ManageOrganizations));
        }
    }
}

// ------------------------------------------------------------------------------------------------
// --------THIS IS AN AUTOMATICALLY GENERATED FILE. ANY MANUAL CHANGES WILL BE OVERWRITTEN!--------
// ------------------------------------------------------------------------------------------------
