{
  "allOf": [
    {
      "$ref": "#/components/schemas/Namespace"
    },
    {
      "type": "object",
      "properties": {
        "listed": {
          "type": "boolean"
        },
        "packageMaintainer": {
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ThingReference"
          }
        },
        "packageOwner": {
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ThingReference"
          }
        },
        "packageType": {
          "$ref": "#/components/schemas/ThingReference"
        },
        "version": {
          "type": "array",
          "items": {
            "$ref": "#/components/schemas/ThingReference"
          }
        },
        "visibility": {
          "type": "string",
          "enum": [
            "PUBLIC",
            "INTERNAL",
            "PRIVATE"
          ]
        }
      },
      "required": [
        "listed",
        "packageMaintainer",
        "packageOwner",
        "packageType",
        "version",
        "visibility"
      ]
    }
  ]
}