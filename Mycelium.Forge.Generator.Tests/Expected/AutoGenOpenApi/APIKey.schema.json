{
  "allOf": [
    {
      "$ref": "./Thing.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "expiresAt": {
          "type": "string",
          "format": "date-time"
        },
        "lastUsedAt": {
          "type": "string",
          "format": "date-time"
        },
        "name": {
          "type": "string"
        },
        "permissions": {
          "$ref": "./ThingReference.schema.json"
        },
        "revokedAt": {
          "type": "string",
          "format": "date-time"
        },
        "secretHash": {
          "type": "array",
          "items": {
            "type": "integer"
          }
        }
      },
      "required": [
        "expiresAt",
        "lastUsedAt",
        "name",
        "permissions",
        "revokedAt",
        "secretHash"
      ]
    }
  ]
}