@inherits LayoutComponentBase

<div class="min-h-screen flex flex-col bg-background text-foreground @(this.ThemeService.IsDarkMode ? "dark" : "")">
    <Header IsDocumentation="true"></Header>

    <main class="flex-1 w-full">
        @(this.Body)
    </main>

    <Footer></Footer>
</div>